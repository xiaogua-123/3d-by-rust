# 调试参数计划

---

## 一、速度参数

### 已配置化

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `move_speed` | 2.5 | `player.rs:28` | 玩家移动速度 (单位/秒) |
| `rotation_speed` | π×4 (12.56) | `player.rs:31` | 模型转向速度 (弧度/秒) |
| `collectible_float_speed` | 3.0 | `config.rs:18` | 收集品浮动频率 |
| `collectible_rotation_speed` | 2.0 | `config.rs:20` | 收集品自转速度 (弧度/秒) |
| `enemies[].speed` | 2.0~3.0 | `zones/*.ron` | 敌人巡逻速度 |

### 硬编码

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| 敌人攻击冷却 | 1.0s | `level.rs:446` | `Timer::from_seconds(1.0)` |
| 敌人巡逻到达判定距离 | 0.3 | `enemy.rs:36` | `dist < 0.3` |
| 玩家动画速度 | 1.0 | `animation.rs` | 动画播放速率 |

### 调整步骤

#### 调整玩家移动速度 `move_speed`

1. 打开 `ni/src/player.rs`
2. 找到 `PlayerSettings::default()` 中的 `move_speed: 2.5`（第28行）
3. 修改数值：
   - **太快**（>5.0）→ 角色瞬移感，难以精确控制
   - **太慢**（<1.0）→ 角色迟钝，地图感觉过大
   - **推荐范围**：1.5 ~ 4.0
4. 保存 → `cargo run` 测试

#### 调整模型转向速度 `rotation_speed`

1. 打开 `ni/src/player.rs`
2. 找到 `rotation_speed: std::f32::consts::PI * 4.0`（第31行）
3. 修改数值：
   - 值越大 → 模型瞬间转向移动方向
   - 值越小 → 模型缓慢回头，有惯性感
   - **推荐范围**：π×2 ~ π×8（6.28 ~ 25.13）
4. 保存 → 跑起来 + 快速按反方向键测试

#### 调整收集品浮动速度

1. 打开 `ni/src/config.rs`
2. 找到 `collectible_float_speed: 3.0`（第18行）
   - 越大 → 上下浮动越快（像心跳）
   - 越小 → 浮动越慢（像漂浮）
3. 同文件 `collectible_float_amplitude: 0.1`（第19行）控制浮动幅度
4. 同文件 `collectible_rotation_speed: 2.0`（第20行）控制自转速度

#### 调整敌人巡逻速度

1. 打开 RON 文件（如 `assets/zones/grassland.ron`）
2. 找到 `enemies: [` 下的 `speed` 字段
3. 每个敌人独立设置速度

#### 调整敌人攻击冷却（硬编码→配置化想改）

1. 打开 `ni/src/level.rs`
2. 找到第446行: `Timer::from_seconds(1.0, TimerMode::Once)`
3. 将 `1.0` 改为需要的冷却秒数：
   - 更短（0.5）→ 敌人攻击频繁，难度↑
   - 更长（3.0）→ 敌人攻击间隔大，难度↓

### 可能遇到的问题

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| move_speed 和 gravity 不匹配 | 角色在空中飘很远（低重力+高速度） | 计算公式：跳跃水平距离 ≈ move_speed × 2 × jump_force / \|gravity\|。保持比值合理 |
| rotation_speed 过大 | 角色频繁抖动（微小移动就转头） | 降低到 π×3~4，或在 `apply_player_rotation` 中加入最小角度阈值 |
| 收集品浮动太快 | 金色球体疯狂抖动 | 降低 `collectible_float_speed` 到 1.5~2.0 |
| 动画速度与移动不匹配 | 脚步滑动/月球漫步 | 检查动画播放速率是否与 move_speed 成比例 |
| 敌人巡逻过快 | 敌人疯狂来回跑 | 降低 RON 中 enemy 的 `speed`，2.0~3.0 是适中范围 |
| 修改 PlayerSettings 不生效 | 改完代码没变化 | 确保修改的是 `default()` 而非运行时值；也可以检查 `sync_flashlight_from_settings` 的限制 |

---

## 二、物体大小参数

### 已配置化

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `pickup_radius` | 1.0 | `config.rs:15` | 收集品拾取触发半径 |
| `enemy_damage_radius` | 1.2 | `config.rs:16` | 敌人伤害触发半径 |
| `player_radius` | 0.3 | `config.rs:21` | 玩家碰撞半径 (XZ圆形) |
| `player_height` | 0.5 | `config.rs:22` | 玩家碰撞高度 |
| `floor_size` | 60~100 | `zones/*.ron` | 地面边长 |
| `walls[].scale` | 各区不同 | `zones/*.ron` | 墙壁缩放 (X,Y,Z) |
| `transitions[].trigger_size` | 各区不同 | `zones/*.ron` | 过渡触发区域 (宽,高,深) |

### 硬编码

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| 平台半尺寸 | 1.5×0.15×1.5 | `level.rs:360` | `Cuboid::new(3.0, 0.3, 3.0)` |
| 收集品球半径 | 0.3 | `level.rs:416` | `Sphere::new(0.3)` |
| 敌人立方体半尺寸 | 0.4×0.4×0.4 | `level.rs:435` | `Cuboid::new(0.8, 0.8, 0.8)` |
| NPC立方体半尺寸 | 0.3×0.8×0.3 | `level.rs:388` | `Cuboid::new(0.6, 1.6, 0.6)` |
| NPC对话触发半径 | 2.5 | `level.rs:386` | 硬编码 |
| 墙壁基础半尺寸 | 0.5×0.5×0.5 | `level.rs:373` | `Cuboid::new(1.0,1.0,1.0)` 再乘scale |
| 墙壁碰撞半尺寸 | 0.5×0.5×0.5 | `level.rs:377` | 固定值，未乘scale⚠️ |

### 调整步骤

#### 调整玩家碰撞体积 `player_radius` / `player_height`

1. 打开 `ni/src/config.rs`
2. 找到 `player_radius: 0.3`（第24行）和 `player_height: 0.5`（第25行）
3. 调整规则：
   - `player_radius` 越大 → 玩家与墙/物体的间距越大，窄通道可能无法通过
   - `player_height` 越大 → 判定玩家"高度"，影响头顶碰撞（当前未使用头顶碰撞）

#### 调整收集品拾取半径 `pickup_radius`

1. 打开 `ni/src/config.rs`
2. 找到 `pickup_radius: 1.0`（第18行）
3. 调整规则：
   - 太大（>3.0）→ 路过就自动捡起，失去"走过去捡"的游戏性
   - 太小（<0.5）→ 需要精确对准，手感差
   - **推荐范围**：0.8 ~ 2.0

#### 调整敌人伤害半径 `enemy_damage_radius`

1. 打开 `ni/src/config.rs`
2. 找到 `enemy_damage_radius: 1.2`（第19行）
3. 调整规则：
   - 越大 → 敌人远程就能伤到玩家
   - 越小 → 必须贴脸才受伤

#### 调整地面大小 `floor_size`（程序化地图）

1. 打开 RON 文件（如 `assets/zones/grassland.ron`）
2. 修改 `floor_size` 值
3. 注意事项：
   - 增大地面后所有物体（NPC/收集品/敌人/平台）仍然在原来的坐标范围内
   - 需要同步扩展物体分布范围，否则大部分地面是空的
   - 过渡触发器的 `trigger_size` X 值应 ≈ floor_size

#### 调整墙壁尺寸（程序化地图）

1. 打开 RON 文件（如 `assets/zones/dark_ruins.ron`）
2. 修改 `walls[].scale` 元组
3. ⚠️ 重要：碰撞半尺寸在代码中硬编码为 `(0.5, 0.5, 0.5)`，修改 scale 后**视觉和碰撞会不同步**
4. 需要同步修改 `level.rs:377` 的碰撞半尺寸 = `(0.5*s.x, 0.5*s.y, 0.5*s.z)`

#### 调整硬编码物体尺寸（需改代码）

| 物体 | 代码位置 | 改法 |
|------|---------|------|
| 平台 | `level.rs:375` | 改 `Cuboid::new(w, h, d)` 三个参数 |
| 收集品球 | `level.rs:430` | 改 `Sphere::new(radius)` |
| 敌人立方体 | `level.rs:451` | 改 `Cuboid::new(w, h, d)` |
| NPC立方体 | `level.rs:402` | 改 `Cuboid::new(w, h, d)` |
| NPC对话触发半径 | `level.rs:400` | 改 `radius` 变量值 |

### 可能遇到的问题

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| pickup_radius 太大 | 隔着墙也能捡到收集品 | 降低到 1.0~1.5，确保拾取系统考虑碰撞 |
| player_radius 太大 | 无法通过墙壁之间的缝隙 | 降低到 0.2~0.3，或增大墙壁间距 |
| floor_size 改了但地图没变大 | 使用城市 GLB 地图，floor_size 只影响碰撞面 | GLB 场景大小由 3D 模型决定，需改 scale（见第十一节） |
| 墙壁 scale≠1 但碰撞不变 | 视觉墙大但能穿过去（或反之） | 修改 `level.rs:377` 碰撞半尺寸乘以 scale |
| NPC 对话触发半径 2.5 | 离很远就开始对话 | 降低到 1.5~2.0 |
| 过渡区 trigger_size 太小 | 走到地图边缘也不切换区域 | 检查 trigger_size X 是否覆盖整个地图宽度 |

---

## 三、视角参数

### 已配置化

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `mouse_sensitivity` | 0.002 | `player.rs:32` | 鼠标视角灵敏度 |

### 硬编码

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| 相机偏移 | (0, 1.9, -1.0) | `player.rs:160` | 相机相对玩家位置 (第三人称) |
| 俯仰角上限 | ±1.5 rad (±86°) | `camera.rs:76` | `.clamp(-1.5, 1.5)` |
| 光标解锁按键 | O | `camera.rs:47` | 按下O切换光标锁定/自由 |
| 默认FOV | Bevy默认 | `player.rs:159` | `Camera3d::default()` ~90°水平FOV |
| 默认近裁剪面 | Bevy默认 | — | 约0.1 |
| 默认远裁剪面 | Bevy默认 | — | 约1000 |

### Camera3d 可配置属性（当前用default，可扩展为PlayerSettings）

| 参数 | Bevy默认 | 说明 |
|------|----------|------|
| `fov` | ~90° (水平) | 视野角度，越小越放大 |
| `near` | 0.1 | 近裁剪面距离 |
| `far` | 1000.0 | 远裁剪面距离 |

### 调整步骤

#### 调整鼠标灵敏度 `mouse_sensitivity`

1. 打开 `ni/src/player.rs`
2. 找到 `mouse_sensitivity: 0.002`（第32行）
3. 调整规则：
   - 值越大 → 鼠标越灵敏，轻移就转大角度
   - 值越小 → 鼠标越"重"，需要大幅度移动
   - **推荐范围**：0.001 ~ 0.005
4. 测试：锁定光标后左右晃鼠标，感受转向速度

#### 调整相机偏移（第三人称距离/高度）

1. 打开 `ni/src/player.rs`
2. 找到 `Transform::from_xyz(0.0, 1.9, -1.0)`（第160行）
3. 三个坐标含义：
   - `x`：水平偏移（通常保持 0）
   - `y`：相机高度 — 越大越高，角色看起来更小
   - `z`：相机前后距离 — 负值=在角色身后，绝对值越大越远
4. 常见调整：
   - 想看更多场景 → `z=-2.5`（拉远），`y=3.0`（抬高）
   - 想沉浸感更强 → `z=-0.5`（贴近），`y=1.5`（平视）

#### 调整俯仰角限制

1. 打开 `ni/src/camera.rs`
2. 找到 `.clamp(-1.5, 1.5)`（第76行）
3. 调整规则：
   - 上限 1.5 rad ≈ 86°（接近正上方）
   - 增大到 1.57（90°）→ 可以看正上方天空
   - 缩小到 1.0（57°）→ 限制视角更水平
   - 负数下限决定能向下看多少

#### 调整 FOV（视野角度）

1. 打开 `ni/src/player.rs`
2. 找到 `Camera3d::default()` → 需改为手动指定 FOV：

```rust
// player.rs:159 — 替换 Camera3d::default()
Camera3d {
    fov: 60.0_f32.to_radians(),  // 60°垂直FOV（小=放大，大=广角）
    ..default()
},
```

3. 调整效果：
   - 小 FOV（45°~60°）→ 放大感，适合观察远处细节
   - 大 FOV（90°~110°）→ 广角感，适合快速移动/战斗
   - 推荐第三人称：60°~75°

#### 调整光标解锁按键

1. 打开 `ni/src/camera.rs`
2. 找到 `KeyCode::KeyO`（第47行）
3. 改为 `KeyCode::Escape` / `KeyCode::Tab` 等

### 可能遇到的问题

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| 鼠标灵敏度突然失效 | 光标没有锁定 | 检查是否在 Playing 状态，按 O 切换锁定；确保 `CursorGrabMode::Locked` 生效 |
| 相机穿墙/穿地 | 视角移到墙后/地下 | 当前无碰撞检测，可后续添加 raycast 相机避障 |
| 俯仰角 86° 还不够 | 想抬头看天空 | 增大 clamp 上限到 1.57（90°），但 >90° 相机可能翻转 |
| 相机太近模型遮挡 | 靠近墙时看到墙内 | 增加相机 z 偏移（拉远），或添加相机碰撞 |
| FOV 太大画面畸变 | 边缘物体拉伸 | 降低 FOV 到 60°~70°，畸变通常在 >100° 才明显 |
| 对话框时视角仍可动 | 对话时鼠标还能转视角 | `camera.rs` 已经限制 `run_if(in_state(GamePhase::Playing))`，确认对话状态正确切换 |

---

## 四、光照参数

### 已配置化 (PlayerSettings)

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `flashlight_intensity` | 800000 | `player.rs:33` | 手电筒聚光亮度 |
| `flashlight_range` | 20.0 | `player.rs:34` | 手电筒照射距离 |
| `flashlight_color` | (1.0, 0.95, 0.8) | `player.rs:35` | 手电筒色温 |

### 硬编码 — 场景光照 (world.rs)

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| 环境光亮度 | 15.0 | `world.rs:39` | `ambient.brightness` |
| 环境光颜色 | (0.04, 0.04, 0.06) | `world.rs:40` | 微蓝黑，低环境光配合三渲二 |
| 方向光颜色 | (1.0, 0.95, 0.85) | `world.rs:18` | 暖白阳光 |
| 方向光照度 | 8000 lux | `world.rs:19` | `illuminance: 8000.0` |
| 方向光旋转X | -0.8 rad (-46°) | `world.rs:22` | 太阳高度角 |
| 方向光旋转Y | 0.5 rad (29°) | `world.rs:22` | 太阳方位角 |
| 点光源颜色 | (0.6, 0.7, 0.9) | `world.rs:29` | 冷色补光 |
| 点光源强度 | 500000 | `world.rs:30` | CD单位 |
| 点光源范围 | 200.0 | `world.rs:31` | 覆盖距离 |
| 点光源位置 | (0, 5, 0) | `world.rs:34` | 场景正上方 |

### 硬编码 — 手电筒 (player.rs)

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| 手电筒外角 | 0.5 rad (29°) | `player.rs:169` | `outer_angle: 0.5` |
| 手电筒偏移 | (0, 1.5, 0.5) | `player.rs:172` | 相对玩家位置 |

### 调整步骤

#### 调整场景环境光（全局亮度底色）

1. 打开 `ni/src/world.rs`
2. 找到 `setup_world` 函数：
   - `ambient.brightness: 15.0`（第39行）→ 值越大场景越亮，暗处可见度越高
   - `ambient.color: (0.04, 0.04, 0.06)`（第40行）→ 环境光色调（当前微蓝黑）
3. 三渲二建议：
   - 环境光不宜太高（否则失去明暗对比）
   - 范围：5 ~ 30，三渲二推荐 10~20
4. 测试：走到地图边缘暗处，看能否看清地面

#### 调整太阳光（方向光）

1. 打开 `ni/src/world.rs`
2. 找到 `DirectionalLight`：
   - `color: (1.0, 0.95, 0.85)`（第18行）→ 暖白，改绿色=森林感，改冷白=科幻感
   - `illuminance: 8000.0`（第19行）→ 照度 lux，越大越亮
   - 旋转 X=-0.8（第22行）→ 太阳高度，-0.8≈从偏上方45°照来
   - 旋转 Y=0.5（第22行）→ 太阳方位

3. 调整太阳角度效果：
   ```
   X 旋转:
     -0.3  → 太阳在头顶（正午，阴影短）
     -1.0  → 太阳在低角度（黄昏，阴影长）
     -1.57 → 太阳在地平线（日出/日落）
   ```

#### 调整补光（点光源）

1. 打开 `ni/src/world.rs`
2. 找到 `PointLight`：
   - `color: (0.6, 0.7, 0.9)` → 冷色补光，平衡暖色太阳光
   - `intensity: 500_000.0` → CD 单位，越大越亮
   - `range: 200.0` → 照射覆盖范围
   - `position: (0, 5, 0)` → 场景正上方 5 单位

#### 调整手电筒

1. 打开 `ni/src/player.rs`
2. 在 `PlayerSettings::default()` 中修改：
   - `flashlight_intensity: 800_000`（第33行）→ 亮度
   - `flashlight_range: 20.0`（第34行）→ 照射距离
   - `flashlight_color: (1.0, 0.95, 0.8)`（第35行）→ 暖白手电筒色

3. 手电筒锥角（硬编码，`player.rs:169`）：
   - `outer_angle: 0.5` → 0.5 rad = 29°，越小越聚焦
   - 0.3 = 17°（窄光束，像激光笔）
   - 0.8 = 46°（宽光束，像泛光灯）

4. 手电筒偏移（`player.rs:172`）：
   - `(0, 1.5, 0.5)` → 在玩家头顶 1.5 处、前方 0.5
   - 调整 Y 可改变灯光高度
   - 调整 Z 可让光更远/更近

### 可能遇到的问题

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| 场景整体太暗 | 地面/墙壁接近全黑 | ↑ ambient.brightness（15→25），或 ↑ illuminance |
| 场景太亮/没阴影 | 没有三渲二的明暗对比 | ↓ ambient.brightness（15→8），↓ 点光源 intensity |
| 阴影边缘太硬 | 三渲二 ramp 配合方向光产生生硬过渡 | 调整 ramp 纹理（增加灰阶层级） |
| 手电筒不亮 | 打开手电筒看不到光 | ↑ flashlight_intensity（800000→2000000），但注意 SpotLight 在有方向光的环境下可能不明显 |
| 手电筒范围太小 | 远处完全看不到 | ↑ flashlight_range（20→50） |
| 方向光方向不对 | 阴影方向奇怪 | 调整旋转 X 和 Y 的组合，注意 Quat 乘法顺序 |
| 环境光颜色影响色调 | 场景偏蓝或偏暖 | 改 ambient.color，偏暖=(0.06,0.04,0.03)，偏冷=(0.03,0.04,0.06) |
| 光照改了没效果 | 重启后还是原来的亮度 | `world.rs` 只在 `Startup` 执行，需要重启程序 |

---

## 五、渲染风格参数 (Toon Shading)

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `default_spec_threshold` | 0.8 | `toon/mod.rs:27` | 高光阈值，越小高光越多 |
| `default_spec_smoothness` | 0.01 | `toon/mod.rs:28` | 高光边缘硬度 |
| `default_spec_color` | WHITE | `toon/mod.rs:29` | 高光颜色 |
| `default_outline_color` | BLACK | `toon/mod.rs:30` | 描边颜色 |
| `default_outline_width` | 0.03 | `toon/mod.rs:31` | 描边宽度 |

### 调整步骤

#### 调整高光阈值 `default_spec_threshold`

1. 打开 `ni/src/toon/mod.rs`
2. 找到 `default_spec_threshold: 0.8`（第27行）
3. 调整效果：
   - 接近 0.0 → 几乎所有表面都有高光（油腻感）
   - 接近 0.8 → 只有很亮的表面有高光（干净感）
   - 1.0 → 完全没有高光（全哑光）
4. 推荐：三渲二风格 0.7~0.85

#### 调整高光边缘硬度 `default_spec_smoothness`

1. 打开 `ni/src/toon/mod.rs`
2. 找到 `default_spec_smoothness: 0.01`（第28行）
3. 调整效果：
   - 0.0 → 高光边缘像刀切（硬朗卡通风）
   - 0.1 → 高光边缘柔和（写实过渡）
4. 推荐：三渲二保持 0.0~0.02

#### 调整描边 `default_outline_width`

1. 打开 `ni/src/toon/mod.rs`
2. 找到 `default_outline_width: 0.03`（第31行）
3. 调整效果：
   - 0.01 → 极细描边（精致）
   - 0.03 → 标准描边（当前）
   - 0.06 → 粗描边（强烈漫画感）
4. 修改 `default_outline_color`（第30行）可改描边颜色

#### 调整 ToonSettings 整体

1. Build 阶段加载的 ToonSettings 从 `assets/toon_settings.ron` 读取（如果有的话）
2. 如果没有 RON 文件，使用代码中的 `default()` 值
3. 可通过修改 `toon/mod.rs` 的 `Default` 实现来批量调整

### 可能遇到的问题

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| 高光太多/太亮 | 角色表面反光强烈，不像卡通 | ↑ spec_threshold 到 0.9+，或改 spec_color 为灰色 |
| 高光完全消失 | 所有物体都是哑光的 | ↓ spec_threshold 到 0.3~0.5 |
| 描边断裂/不连续 | 模型某些边缘没有描边 | 检查模型法线方向，背面剔除可能影响描边渲染 |
| 描边太粗遮住细节 | 小物体变成一团黑 | ↓ outline_width 到 0.01~0.015 |
| ramp 纹理缺失 | 三渲二没有分级明暗效果 | 确认 `ramp_handle` 是否正确加载，检查 `toon/ramp.rs` |
| 不同材质的高光不一致 | 场景中物体高光效果不一 | 检查每个材质创建时是否使用了正确的 spec_threshold/spec_smoothness |

### 渲染管线调试方法

| 方法 | 说明 |
|------|------|
| 临时关闭 toon | 在 `make_toon_material` 中设 `toon_enabled: 0` |
| 调试描边 | 设 outline_width=0.1 使描边极度明显 |
| 调试高光 | 设 spec_threshold=0.1 使高光全显 |
| 调试 ramp | 替换 ramp 纹理为彩虹色条确认加载 |

---

## 六、Zone 全区参数一览

| 参数 | grassland | blue_forest | dark_ruins | city |
|------|-----------|-------------|------------|------|
| floor_size | 60 | 80 | 60 | 100 |
| GLB场景 | 无 | 无 | 无 | models/city.glb |
| 收集品 | 3 | 5 | 7 | 5 |
| 敌人 | 1 (spd 2.0) | 2 (spd 2.5/2.0) | 2 (spd 3.0/2.5) | 2 (spd 2.5/2.0) |
| NPC | 1 (村长) | 1 (旅人) | 0 | 1 (市民) |
| 平台 | 4 | 0 | 0 | 0 |
| 墙壁 | 0 | 0 | 6 (迷宫) | 0 |
| 过渡 | → blue_forest | ← grass / → ruins | ← blue_forest | → grassland |

### 调整步骤

#### 添加新区域

1. 创建 `assets/zones/<新id>.ron` 文件
2. 在 `ni/src/level.rs` 的 `GameLevel` 枚举中添加新关卡：
   ```rust
   pub enum GameLevel {
       // ... 现有 ...
       Level5,
   }
   ```
3. 在 `GameLevel::zone_id()` 中添加映射：
   ```rust
   GameLevel::Level5 => "new_zone_id",
   ```
4. 在 `GameLevel::from_zone_id()` 中添加反向映射
5. 在 `GameLevel::display_name()` 中添加中文名
6. 添加 `spawn_level5` 函数和对应的 `OnEnter` / `OnExit` 系统
7. 在 `LevelPlugin::build()` 中注册

#### 修改现有区域的物体布局

1. 打开对应 RON 文件
2. 调整数值：
   - 收集品位置 → 改 `collectibles: [(x,y,z), ...]`
   - 敌人巡逻路径 → 改 `enemies[].patrol: [(x,y,z), ...]`
   - NPC 位置 → 改 `npcs[].position`
   - 墙壁位置 → 改 `walls[].position` + `scale`
   - 过渡区 → 改 `transitions[].trigger_pos` + `trigger_size` + `spawn_point`
3. 保存 → 启动游戏，跳到对应关卡测试

#### 调整 Zone 区域切换

1. 打开源区域和目标区域两个 RON 文件
2. 源区域的 `transitions` 中定义"从哪出发"
3. 目标区域的 `spawn_point` 定义"到达哪里"
4. **关键**：目标 spawn_point 必须在目标过渡触发区**之外**（至少 5~10 单位），否则会触发无限循环切换
5. 两点距离参考：
   - grassland ↔ blue_forest：spawn 点距离边框 ~24 单位
   - blue_forest ↔ dark_ruins：spawn 点距离边框 ~25~30 单位
   - city → grassland：spawn 点距离边框 ~24 单位

#### 切换 GLB 模型 / 新增模型

1. 将 `.glb` 文件放入 `assets/models/`
2. 在 RON 中设置 `glb_scene: Some("models/新文件.glb#Scene0")`
3. 首次加载需等待异步 IO（1~N 帧）
4. 如需碰撞，在 RON 中添加 `walls` 数组手动定义碰撞体

### 可能遇到的问题

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| 新区域加载后地面全黑 | 看不到地面 | 检查 `floor_color` 格式 `(r, g, b)` 取值范围 0~1 |
| 区域切换后玩家出现在错误位置 | 一进入新区域就在过渡区边缘 | 检查 `transitions[].spawn_point` 是否在目标区过渡触发器外 |
| 两个区域之间无限循环切换 | 角色来回弹跳 | spawn_point 离目标区过渡触发器太近，增大距离 |
| RON 语法错误导致区域加载失败 | 启动时报错 "解析区域文件失败" | 检查 RON 语法：括号匹配、逗号、元组格式 |
| zone_id 不匹配 | 关卡切换后空白 | 检查 `GameLevel::zone_id()` 返回值与 `.ron` 文件 `id` 字段一致 |
| GLB 模型路径错误 | 场景加载后只有碰撞面没有建筑 | 路径必须带 `#Scene0`；确认文件存在于 `assets/models/` |
| 收集品/敌人在错误的位置 | 物体嵌入地面或飘在空中 | 调整 Y 坐标：地面 y=0.0，物体 y 应 >0（收集品 y≥0.3，敌人 y≥0.4）|

---

## 七、地图加载机制参数

### 加载时序

```
Startup
  └─ load_zones()           ← 扫描 assets/zones/*.ron，解析 ZoneDef → ZoneBank
                               注意: 只加载定义数据，不加载 3D 资源

主菜单 → 点击"开始游戏"
  └─ StartGameEvent
       └─ handle_start_game_level()
            └─ state = Level1 → OnEnter(Level1)
                 └─ spawn_level1() → spawn_zone()
                      ├─ GLB: asset_server.load() ← 触发异步加载，本帧返回 Handle
                      ├─ 程序化地面/平台/墙壁/NPC/敌人/收集品 ← 同步生成
                      └─ ZoneTransition 触发器 ← 同步生成

关卡切换 (玩家走入过渡区)
  └─ check_zone_transition()          ← Update 阶段每帧检测
       └─ LoadLevelEvent
            ├─ OnExit(旧) → cleanup_level()   ← 销毁所有带 LevelEntity 的实体
            ├─ OnEnter(新) → spawn_zone()     ← 生成新区域
            └─ ResetPlayerEvent               ← 传送玩家到生成点
```

### 加载机制关键参数

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| zone 定义目录 | `assets/zones/` | `level.rs:291` | 启动时扫描 |
| zone 文件格式 | `.ron` (RON) | `level.rs:296` | Rust Object Notation |
| 加载时机 | Startup | `level.rs:253` | `.add_systems(Startup, load_zones)` |
| 生成时机 | OnEnter(GameLevel) | `level.rs:254-257` | Bevy 状态进入回调 |
| 清理时机 | OnExit(GameLevel) | `level.rs:258-262` | 状态退出时销毁 |
| 清理标记 | `LevelEntity` 组件 | `level.rs:280-284` | 所有区域实体都带此标记 |
| 非游戏状态清理 | OnEnter(GameOver/MainMenu) | `level.rs:264-265` | 游戏结束/主菜单时清空关卡 |
| GLB 加载方式 | `asset_server.load()` | `level.rs:344` | Bevy 异步资产加载，非阻塞 |
| 程序化地面 | `Plane3d` + `floor_size` | `level.rs:361-362` | GLB 缺失时自动生成 |

### 区域过渡参数

| 来源 → 目标 | 触发位置 (trigger_pos) | 触发尺寸 (trigger_size) | 目标生成点 (spawn_point) |
|-------------|------------------------|-------------------------|--------------------------|
| grassland → blue_forest | (0, 1, -29) | (60, 3, 2) | (0, 0, 25) |
| blue_forest → grassland | (0, 1, 39) | (80, 3, 2) | (0, 0, -24) |
| blue_forest → dark_ruins | (0, 1, -39) | (80, 3, 2) | (0, 0, 25) |
| dark_ruins → blue_forest | (0, 1, 29) | (60, 3, 2) | (0, 0, -30) |
| city → grassland | (0, 1, 49) | (100, 3, 2) | (0, 0, -24) |

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| 触发判定算法 | AABB 点包含 | `level.rs:137-151` | `player_in_box()` |
| 触发尺寸 Y 统一值 | 3.0 (高6米) | 各 `.ron` | 确保跳起也能触发 |
| 触发尺寸 Z 统一值 | 2.0 (深4米) | 各 `.ron` | 过渡带深度 |
| 触发尺寸 X | = floor_size | 各 `.ron` | 覆盖整个地图宽度 |
| 目标 spawn_point 距离边界 | 24~30 单位 | 各 `.ron` | 防止二次触发回退 |

### GLB 场景加载

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| city 场景路径 | `models/city.glb#Scene0` | `city.ron:4` | 必须以 `#Scene0` 结尾 |
| GLB Feature | `bevy_gltf` | `Cargo.toml:19` | bevy 的 glTF/GLB 支持 |
| 加载方式 | 同步返回 Handle + 异步 IO | `level.rs:344` | 可能延迟 1~N 帧才显示 |
| GLB 碰撞体 | Plane { y: 0.0 } 仅地面 | `level.rs:348-352` | 建筑/街道模型无碰撞 |
| 程序化备选 | floor_size + floor_color | `level.rs:354-368` | GLB=None 时自动启用 |
| Bevy 0.18 要求 | 路径必须带 `#Scene0` 标签 | `level.rs:336-340` | 否则加载失败 |

### 玩家位置重置

| 参数 | 触发方式 | 位置 | 说明 |
|------|----------|------|------|
| 关卡进入 | ResetPlayerEvent | `level.rs:228-230` | position=(0,0,0), rotation=IDENTITY |
| 区域过渡 | ResetPlayerEvent + spawn_point | `level.rs:517-519` | 使用 Zone 定义的 spawn_point |
| 新游戏 | ResetPlayerEvent::default() | `level.rs:577` | 回到原点 |
| 下一关 | ResetPlayerEvent::default() | `level.rs:597` | 回到原点 |

### 区域切换边界情况

| 场景 | 处理方式 | 位置 |
|------|----------|------|
| 同一区域反复触发 | `trigger.target_zone != config.current_level` 守卫 | `level.rs:125` |
| 游戏结束 | `OnEnter(GamePhase::GameOver)` → 清空关卡状态 | `level.rs:264` |
| 返回主菜单 | `OnEnter(GamePhase::MainMenu)` → 清空关卡状态 | `level.rs:265` |
| 通关所有关卡 | `.next() == None` → 日志提示 | `level.rs:599-601` |
| 无效过渡目标 | `error!("未知目标区域: {}")` | `level.rs:487` |
| zone 文件缺失 | `error!("找不到区域定义: {}")` | spawn_levelX 函数中 |
| zones 目录不存在 | 自动创建 + error 日志 | `level.rs:316-317` |

### 调整步骤

#### 诊断地图加载问题

1. 启动程序，观察控制台日志
2. 关键日志标识：
   - `加载区域: xxx (xxx)` → 区域定义加载成功
   - `GLB 场景: xxx` → GLB 模型路径被识别
   - `区域 xxx 已加载` → 区域实体已生成
   - `找不到区域定义: xxx` → RON 文件缺失或 id 不匹配
3. 使用 F3 调试面板 → 实体标签 → 查看 LevelEntity 是否被创建

#### 手动触发关卡切换

1. 游戏中按数字键 `1/2/3/4` 直接跳到对应关卡
2. 此功能仅在 debug 模式下生效（`debug_level_switch` 系统）

#### 调整 GLB 加载路径

1. 确认 GLB 文件在 `assets/models/` 下
2. 检查 RON 文件中路径格式：`glb_scene: Some("models/文件名.glb#Scene0")`
3. 代码在 `level.rs:336-340` 自动补全 `#Scene0`：
   ```rust
   let path = if glb_path.contains('#') {
       glb_path.clone()
   } else {
       format!("{glb_path}#Scene0")
   };
   ```
4. 如果路径已有 `#`，不会重复添加

#### 解决区域过渡问题

1. 检查 `transitions[].trigger_size` 是否覆盖地图边缘
2. 检查 `trigger_pos` Z 值是否在地图边界附近
3. 检查 `spawn_point` 是否在目标区域过渡区之外
4. 计算公式：`|新spawn_point.z - 目标过渡区trigger_pos.z| ≥ 5`

### 可能遇到的问题

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| GLB 模型加载了但不显示 | 场景有碰撞但看不到建筑 | GLB 异步加载延迟，等几秒；或检查模型文件完整性 |
| city.glb 加载卡顿 | 切换城市关卡时画面冻结 | city.glb 约 60MB，首次加载需时间；考虑换用 city1.glb (19MB) |
| 区域切换后地面消失 | 进入新区域看不到地面 | 检查 `cleanup_level` 是否误删了不该删的实体 |
| 多次按数字键切换导致状态混乱 | 实体重复生成 | `cleanup_level` 依赖 `LevelEntity` 标记，确保所有区域实体带此标记 |
| 非游戏状态不清理关卡 | 主菜单/GameOver 后还看到地图 | 检查 `OnEnter(GamePhase::GameOver/MainMenu)` 是否触发 `clear_level_state` |

---

## 八、碰撞检测参数

### 碰撞系统架构

```
碰撞类型        算法                        检测对象
─────────────────────────────────────────────────────
地面检测        Plane/BBox raycast(Y轴)    玩家 vs 地面/平台/墙壁顶部
水平推开        Circle-vs-AABB closest      玩家 vs Box碰撞体
敌人伤害        距离判定 (球形)             玩家 vs 敌人
收集品拾取      距离判定 (球形)             玩家 vs 收集品
地图过渡        AABB 点包含判定             玩家 vs 过渡触发器
```

### 玩家碰撞参数

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `player_radius` | 0.3 | `config.rs:21` | 玩家水平碰撞半径 (XZ圆形) |
| `player_height` | 0.5 | `config.rs:22` | 玩家碰撞体高度 (从脚到头顶) |
| 着地判定阈值 | 0.1 | `player.rs:248` | `player.y ≤ ground_y + 0.1` 就算着地 |
| 跳跃弹起偏移 | 0.2 | `player.rs:269` | 跳跃瞬间将玩家 Y 推到 `ground_y + 0.2` |
| 重力 | -15.0 | `player.rs:29` | 每秒加速度，影响下落速度 |
| 跳跃初速度 | 8.0 | `player.rs:30` | 跳跃瞬时 Y 速度 |
| 落地缓冲(coyote) | 0.08s | `player.rs:36` | 离开平台后仍可跳跃时间 |
| 跳跃缓冲(buffer) | 0.1s | `player.rs:37` | 提前按跳被记住的时间 |

### 敌人碰撞/伤害参数

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `enemy_damage_radius` | 1.2 | `config.rs:16` | 敌人伤害触发距离 (球体) |
| `enemy_default_damage` | 1 | `config.rs:17` | 单次伤害扣血量 |
| 敌人攻击冷却 | 1.0s | `level.rs:446` | 两次伤害间隔 |
| 敌人巡逻到达判定 | 0.3 | `enemy.rs:36` | `dist < 0.3` 认为到达巡逻点 |
| ⚠️ 敌人碰撞体 | **无** | — | 敌人不与墙壁碰撞，直接穿墙 |

### 墙壁/平台碰撞参数

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| ⚠️ 墙壁碰撞半尺寸 | 0.5×0.5×0.5 | `level.rs:377` | **硬编码**，未乘Zone中的scale |
| 平台碰撞半尺寸 | 1.5×0.15×1.5 | `level.rs:363` | 硬编码，与Mesh一致 |
| 地面碰撞 | Plane { y: 0.0 } | `level.rs:335` | 无限大平面，y=0 |
| GLB场景碰撞 | Plane { y: 0.0 } | `level.rs:336` | 仅添加地面平面，模型自身无碰撞 |

### 收集品/过渡区碰撞参数

| 参数 | 默认值 | 位置 | 说明 |
|------|--------|------|------|
| `pickup_radius` | 1.0 | `config.rs:15` | 收集品拾取距离 (球体) |
| `transitions[].trigger_size` | 各区不同 | `zones/*.ron` | 过渡触发区域 (宽,高,深) |
| NPC对话触发半径 | 2.5 | `level.rs:386` | 硬编码，对话触发距离 |

---

### 碰撞参数调优指南

#### 1. 着地判定 (`player.rs:248`)
```
is_grounded = player.y ≤ ground_y + 0.1
```
- `0.1` 太小 → 玩家可能"浮空"不动
- `0.1` 太大 → 玩家在空中被错误判定为着地
- 建议范围: 0.05~0.2

#### 2. 跳跃弹起 (`player.rs:269`)
```
player.y = ground_y + 0.2 (跳起瞬间)
```
- 目的: 确保跳跃瞬间玩家离开地面，不会下一帧被拉回
- `0.2` 太小 → 跳不起来（被地面吸附）
- `0.2` 太大 → 瞬移感
- 建议: ≥ player和地面碰撞精度之和

#### 3. 水平推开 (`collision.rs:83-107`)
```
Circle-vs-AABB closest-point 算法
```
- 玩家半径 `player_radius=0.3` 决定与墙壁的间距
- 越大的半径 → 玩家离墙越远，通道感觉越窄
- 算法能正确处理墙壁直角，但**不能处理斜角/圆角**

#### 4. 墙壁碰撞半尺寸 ⚠️ 已知问题 (`level.rs:377`)
```
碰撞: half_extents = (0.5, 0.5, 0.5)  // 固定值！
Mesh:  scale = zone.walls[].scale       // RON中可变
```
当前代码碰撞半尺寸**未乘以 scale**，导致：
- `scale < 1.0` 的墙 → 碰撞比视觉大 → 玩家被"空气墙"挡住
- `scale > 1.0` 的墙 → 碰撞比视觉小 → 玩家能穿墙

修复方案: 将碰撞半尺寸改为 `(0.5*s.x, 0.5*s.y, 0.5*s.z)`

#### 5. GLB场景碰撞 (`level.rs:334-340`)
当前GLB模型**只有一个地面 Plane**，建筑/街道等模型**没有碰撞体**：
- 城市模型中的建筑可以穿墙
- 需要为GLB模型手动添加Box碰撞体（在Zone中加walls配置）
- 未来可考虑: 从GLB mesh自动生成碰撞体

#### 6. 敌人碰撞体缺失
敌人用 `distance < enemy_damage_radius` 判定伤害，但敌人本身：
- 没有碰撞体 → 敌人可穿过任何墙壁
- 没有与玩家的物理推开 → 重叠后只能靠冷却免伤
- 建议添加: 敌人与墙壁的碰撞检测

---

### 碰撞调试方法

| 方法 | 说明 |
|------|------|
| F3 → 玩家标签 | 查看实时 Y 值和着地缓冲状态 |
| F3 → 实体标签 | 查看所有碰撞体的位置/类型 |
| 故意撞墙 | 感受推开力度和缝隙通过性 |
| 跳平台边界 | 测试着地判定阈值是否合理 |
| 跟敌人重叠 | 验证伤害冷却和穿墙问题 |
| console日志 | 添加 `debug!("ground_y={ground_y}, is_grounded={is_grounded}")` |

### 调整步骤

#### 修复墙壁碰撞半尺寸 bug（重要）

1. 打开 `ni/src/level.rs`
2. 找到第 377 行:
   ```rust
   CollisionShape::Box { half_extents: Vec3::new(0.5, 0.5, 0.5) },
   ```
3. 改为乘以 scale:
   ```rust
   CollisionShape::Box {
       half_extents: Vec3::new(
           0.5 * wall.scale.0,
           0.5 * wall.scale.1,
           0.5 * wall.scale.2,
       ),
   },
   ```
4. 保存测试：找一个 scale≠1 的墙，验证碰撞和视觉一致

#### 调整着地判定灵敏度

1. 打开 `ni/src/player.rs`
2. 找到第 248 行: `player.y <= ground_y + 0.1`
3. 修改 `0.1`：
   - 变小（0.03）→ 必须非常贴近地面才算着地
   - 变大（0.3）→ 容易判定为着地
4. 测试：走台阶边缘、跳平台边界

#### 调整跳跃体验

1. 打开 `ni/src/player.rs`
2. 关键参数组合：
   - `jump_force: 8.0` → 跳跃初速度
   - `gravity: -15.0` → 重力加速度
   - `coyote_time: 0.08` → 离开边缘后仍可跳的时间
   - `jump_buffer_time: 0.1` → 提前按跳被记住的时间
3. 计算公式：最大跳跃高度 = jump_force² / (2 × |gravity|) = 64 / 30 = 2.13 单位
4. 平台高度应 ≤ 最大跳跃高度
5. 测试：反复跳、走边缘、在空中按跳

#### 调整敌人伤害判定

1. 打开 `ni/src/config.rs`
2. 修改 `enemy_damage_radius: 1.2` 和 `enemy_default_damage: 1`
3. 打开 `ni/src/level.rs` 第 446 行修改 `attack_cooldown: Timer::from_seconds(1.0, ...)`
4. DPS = damage / cooldown = 1 / 1.0 = 1 HP/秒（当前很温和）
5. 如需更高难度：↑ damage 或 ↓ cooldown

### 可能遇到的问题

| 问题 | 症状 | 解决方案 |
|------|------|----------|
| 玩家从平台边缘滑落 | 站在边缘突然掉下 | ↑ coyote_time 到 0.12~0.15 |
| 按跳没反应 | 特别是快速连按时 | ↑ jump_buffer_time 到 0.15~0.2 |
| 着地后卡住不动 | 落到地面后无法移动 | 检查 `is_grounded` 判定是否正常，打印 `ground_y` debug |
| 从高处坠落无伤害 | 没有坠落伤害系统 | 当前无实现，可添加：累积 fall_distance，着地时扣血 |
| 敌人伤害判定不准 | 看起来没碰到就受伤 | ↑ enemy_damage_radius 的视觉提示，或添加伤害指示器 |
| 玩家推开墙壁后卡入另一面墙 | 在两个碰撞体之间弹跳 | `push_out_horizontal` 单次计算，极端情况可能求解失败 |
| GLB 城市无碰撞 | 可以在建筑中穿行 | 在 RON 的 walls 中手动为建筑添加 Box 碰撞体 |
| 平台下方检测到着地 | 在平台下方时判定为着地 | `find_ground_y` 是向下射线检测，不区分平台顶面和底面 |

---

## 九、参数调优路线

| 阶段 | 内容 | 关键参数 |
|------|------|----------|
| 1. 手感 | 移动/跳跃/转向 | move_speed, gravity, jump_force, rotation_speed |
| 2. 视角 | 鼠标/相机 | mouse_sensitivity, camera_offset, pitch_clamp |
| 3. 光照 | 环境/方向/手电筒 | ambient.brightness, illuminance, flashlight_* |
| 4. 碰撞 | 玩家/敌人/墙壁 | player_radius, player_height, enemy_damage_radius |
| 5. 物体 | 收集品/平台/墙壁尺寸 | Sphere半径, Cuboid尺寸, wall.scale |
| 6. 地图 | 各区布局与加载 | floor_size, 敌人路径, 收集品位置, trigger_size, spawn_point, glb_scene |
| 7. 渲染 | 三渲二风格 | spec_threshold, outline_width, ramp |

### 每阶段详细操作步骤

#### 阶段 1：手感调优（优先级最高）

**目标**：角色操作舒适，移动/跳跃/转向自然

```
操作步骤：
1. 启动游戏，进入关卡 1（草原村）
2. 前后左右移动，感受 move_speed：
   - 太慢 → ↑ move_speed（2.5→3.5），测试
   - 太快 → ↓ move_speed（2.5→1.8），测试
3. 原地快速转圈，感受 rotation_speed：
   - 有延迟感 → ↑ rotation_speed（12.5→20）
   - 太灵敏 → ↓ rotation_speed（12.5→8）
4. 反复跳跃，感受 jump_force + gravity 组合：
   - 跳跃太矮（跳不上平台）→ ↑ jump_force 或 ↓ gravity
   - 跳跃太飘（浮空时间长）→ ↑ |gravity|（-15→-20）
5. 走平台边缘，测试 coyote_time：
   - 经常"掉下去才意识到" → ↑ coyote_time（0.08→0.15）
6. 记录改动 → 每个值只改一次，避免来回调
```

#### 阶段 2：视角调优

**目标**：鼠标手感合适，相机位置舒服

```
操作步骤：
1. 锁定光标后，左右快速晃动鼠标
2. 调整 mouse_sensitivity：
   - 划完整鼠标垫才转90° → ↑ sensitivity（0.002→0.004）
   - 轻轻一动就转一圈 → ↓ sensitivity（0.002→0.001）
3. 走靠近墙壁/物体，观察相机是否遮挡：
   - 频繁遮挡 → 减小 |z| 或增大 y（拉近抬高）
4. 查看俯仰角限制：
   - 想看到正上方天空 → ↑ clamp 上限（1.5→1.57）
```

#### 阶段 3：光照调优

**目标**：场景明暗适合三渲二风格，手电筒实用

```
操作步骤：
1. 白天/明亮区域测试（world.rs）：
   - 场景太暗 → ↑ ambient.brightness 或 ↑ illuminance
   - 三渲二对比度不够 → ↓ ambient.brightness
2. 暗处测试：
   - 打开手电筒（当前无开关，始终有光）
   - 调整 flashlight_intensity 和 flashlight_range
3. 如果添加昼夜循环，需要动态 ambient + directional
```

#### 阶段 4：碰撞调优

**目标**：碰撞判定准确，无明显的"卡住"或"穿模"

```
操作步骤：
1. 撞各种墙，感受推开力度：
   - 能穿过墙 → 检查墙壁碰撞半尺寸（已知 bug）
   - 被推开太远 → ↓ player_radius
2. 跳各种平台边界：
   - 应该能跳上 → 检查 jump_height ≥ platform_y
   - 站不住 → ↑ coyote_time
3. 跟敌人碰撞：
   - 应该受伤 → 检查 enemy_damage_radius
   - 受伤太快 → ↑ attack_cooldown 或 ↓ damage
4. 窄通道测试：如果 player_radius 太大，可能无法通过
```

#### 阶段 5：物体调优

**目标**：物体大小和位置合理，视觉协调

```
操作步骤：
1. 修改对应 RON 文件
2. 每次只改一个物体的一个属性
3. 保存 → 切换关卡 → 观察
4. 调试时按 1/2/3/4 快速切换关卡
```

#### 阶段 6：地图调优

**目标**：每个区域的布局合理，过渡流畅

```
操作步骤：
1. 玩通整个区域（收集所有收集品/击败敌人）
2. 记录冗余区域（空旷无用）和拥挤区域（物体太多）
3. 调整物体分布
4. 测试所有过渡区是否正常触发
5. 如果添加新区域：走完整创建流程（见第六节"添加新区域"）
```

#### 阶段 7：渲染调优

**目标**：三渲二风格统一，视觉效果好

```
操作步骤：
1. 在高光明显的物体附近观察：
   - 高光太多 → ↑ spec_threshold
   - 高光太少 → ↓ spec_threshold
2. 在不同距离观察描边：
   - 近处描边太粗 → ↓ outline_width
   - 远处描边消失 → 检查 outline 渲染距离
3. 对比不同材质的高光效果，确保一致性
```

---

## 十、参数依赖关系

```
视角:
  mouse_sensitivity × pitch_clamp(±1.5) → 抬头/低头范围
  camera_offset(0,1.9,-1.0) → 第三人称距离与高度

光照:
  ambient.brightness + directional.illuminance → 整体场景亮度
  flashlight.intensity + flashlight.range → 玩家可视范围
  ambient + point_light → 暗处可见度 (三渲二需要强对比)
  light_color × material_color → 最终视觉色调

碰撞:
  player_radius + wall_collision(0.5) × wall_scale → 能否通过缝隙
  player_radius + pickup_radius → 收集体验
  jump_height + platform_y → 能否跳上平台

速度:
  move_speed ← → gravity (高速度+低重力=飘)
  jump_force² / (2 × |gravity|) = 跳跃最大高度
  enemy.speed + attack_cooldown(1.0s) → 躲避难度

渲染:
  spec_threshold + spec_smoothness → 三渲二高光硬度
  outline_width → 描边粗细 (太大显得卡通)
  ramp_texture → 明暗分级 (多级=柔和, 少级=硬朗)

地图加载:
  floor_size → 地面面积 + 收集品/敌人可分布范围
  trigger_size × trigger_pos → 过渡区覆盖范围 (太窄=无法触发)
  spawn_point(新) vs spawn_point(旧) → 距离不够=二次触发回退
  glb_scene ←→ floor_size/floor_color (GLB优先, 无则程序化)
  收集品总数 vs 分布密度 → 关卡难度与时长
  walls[].scale vs 碰撞半尺寸(0.5) → 实际可通行宽度
  enemy.speed × patrol 路径长度 → 巡逻周期
```

### 常见依赖问题速查

| 改动 A | 可能影响 B | 需要同步调整 |
|--------|-----------|-------------|
| ↑ move_speed | 地图感觉变小 | ↑ floor_size 或调整物体分布 |
| ↓ gravity | 跳跃高度增大 | 可能需要调高平台 Y 坐标 |
| ↑ jump_force | 能跳上更高的平台 | 之前跳不上的平台现在能上了 |
| ↓ ambient.brightness | 场景变暗 | ↑ flashlight_intensity 保证暗处可见 |
| ↑ player_radius | 窄通道无法通过 | ↑ wall 间距或在 RON 中扩大通道 |
| ↑ floor_size | 物体分布变稀疏 | ↑ 收集品/敌人数量或重新分布位置 |
| ↑ GLB scale | 模型变大 | 碰撞面需要相应调整；模型细节可能放大失真 |
| 新增 walls | 可能阻挡巡逻路径 | 检查敌人 patrol 路径是否被墙隔断 |
| 修改 camera_offset Z | 相机距离改变 | 可能看到更少/更多场景，影响难度感知 |
| ↓ spec_threshold | 高光增多 | 场景可能显得"油腻"，需同步调 spec_smoothness |

### 调参安全规则

1. **每次只改一个参数** → 测试 → 记录效果 → 再改下一个
2. **改前记下原值**，方便回退
3. **极端值测试**：先把参数拉到极端（如 move_speed=10），确认它有影响，再精细调回
4. **关联参数一起考虑**：改 jump_force 时同时想 gravity
5. **跨区域一致性**：速度/视角/光照调一次适用所有区域；物体布局每个区域独立调

---

## 十一、地图位置与尺寸调整（GLB模型 + 程序化地面）

### 架构说明

地图的生成入口是 `level.rs` 中的 `spawn_zone()` 函数（第322行）。根据 Zone RON 文件中是否配置 `glb_scene`，有两种生成模式：

```
spawn_zone()
  ├─ glb_scene 有值 → GLB 模式（如 city）
  │    └─ SceneRoot + Transform
  └─ glb_scene 为空 → 程序化模式（如 grassland）
       └─ Plane3d + floor_size + floor_color
```

GLB 模式和程序化模式的地面/场景位于不同位置，需分别调整。

---

### 11.1 GLB 模型地图的位置与大小

当前代码中，GLB 场景始终以 `Transform::IDENTITY` 生成（`level.rs:343`）：

```rust
// level.rs:341-346 — 当前代码
commands.spawn((
    SceneRoot(asset_server.load(path)),
    Transform::IDENTITY,          // ← 位置=(0,0,0), 旋转=无, 缩放=(1,1,1)
    LevelEntity,
    Name::new(format!("{}_GLB", zone.id)),
));
```

#### 调整 GLB 模型位置

将 `Transform::IDENTITY` 改为 `Transform::from_xyz(x, y, z)`：

```rust
// 把城市地图向右移 10 单位，向上抬 0.5 单位
Transform::from_xyz(10.0, 0.5, 0.0)
```

#### 调整 GLB 模型大小

使用 `.with_scale(Vec3::new(sx, sy, sz))` 缩放：

```rust
// 把城市地图放大 2 倍
Transform::from_xyz(0.0, 0.0, 0.0).with_scale(Vec3::new(2.0, 2.0, 2.0))

// 只压扁 Y 轴（让建筑变矮）
Transform::from_xyz(0.0, 0.0, 0.0).with_scale(Vec3::new(1.0, 0.5, 1.0))
```

#### 从 RON 文件读取位置和缩放（推荐改进方案）

当前 `ZoneDef` 结构体不支持 GLB 的位置/缩放配置，可以扩展为：

```rust
// 在 ZoneDef 中新增字段（level.rs:22-42）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ZoneDef {
    // ... 现有字段 ...
    #[serde(default)]
    pub glb_transform: Option<GlbTransformDef>,  // 新增
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GlbTransformDef {
    pub position: (f32, f32, f32),
    pub scale: (f32, f32, f32),
}
```

然后在 `spawn_zone()` 中使用：

```rust
// 替换原来的 Transform::IDENTITY
let glb_transform = zone.glb_transform.as_ref()
    .map(|t| Transform::from_xyz(t.position.0, t.position.1, t.position.2)
        .with_scale(Vec3::new(t.scale.0, t.scale.1, t.scale.2)))
    .unwrap_or(Transform::IDENTITY);

commands.spawn((
    SceneRoot(asset_server.load(path)),
    glb_transform,
    LevelEntity,
    Name::new(format!("{}_GLB", zone.id)),
));
```

RON 文件中这样配置：

```ron
(
    id: "city",
    display_name: "城市",
    glb_scene: Some("models/city.glb#Scene0"),
    glb_transform: Some((
        position: (0.0, 0.0, 0.0),
        scale: (2.0, 2.0, 2.0),
    )),
    // ...
)
```

---

### 11.2 程序化地面的大小与位置

程序化地面当前代码（`level.rs:361-368`）：

```rust
// 当前地面始终在原点
Mesh3d(meshes.add(Plane3d::default().mesh().size(zone.floor_size, zone.floor_size))),
Transform::from_xyz(0.0, 0.0, 0.0),   // ← 位置固定
```

#### 调整地面位置

```rust
// 把地面向上抬 0.5（比如和 GLB 模型对齐）
Transform::from_xyz(0.0, 0.5, 0.0)
```

#### 调整地面大小

方法一：修改 RON 文件的 `floor_size`（推荐）

```ron
// grassland.ron — 直接改数值即可
floor_size: 60.0,   // 改为 100.0 就是 100×100 的地面
```

方法二：在 RON 中扩展为 `(width, depth)` 支持非正方形：

```rust
// ZoneDef 中
#[serde(default = "default_floor_size")]
pub floor_size: (f32, f32),    // (宽, 深)

fn default_floor_size() -> (f32, f32) { (60.0, 60.0) }
```

---

### 11.3 地图内物体位置

地图内所有物体的位置都在 RON 文件中配置，使用 `(x, y, z)` 坐标：

| 物体 | RON 字段 | 代码位置 | 坐标系参考 |
|------|---------|---------|-----------|
| 地面 | `floor_size` → 程序化生成 | `level.rs:361-362` | 原点 `(0,0,0)` |
| 平台 | `platforms: [(x,y,z), ...]` | `level.rs:372-381` | 世界坐标 |
| 墙壁 | `walls: [{position, scale}]` | `level.rs:385-395` | 世界坐标 + 缩放 |
| NPC | `npcs: [{position}]` | `level.rs:399-418` | 世界坐标 |
| 收集品 | `collectibles: [(x,y,z)]` | `level.rs:422-437` | 世界坐标 |
| 敌人 | `enemies: [{position, patrol}]` | `level.rs:441-464` | 世界坐标 + 巡逻路径 |
| 过渡区 | `transitions: [{trigger_pos, trigger_size}]` | `level.rs:468-488` | 触发器中心 + 半尺寸 |
| 出生点 | `spawn_point` | `level.rs:228` → `ResetPlayerEvent` | 玩家进入时的坐标 |

---

### 11.4 完整操作步骤

#### 步骤 A：调整 GLB 城市地图大小（当前最快方式）

1. 打开 `ni/src/level.rs`
2. 找到第 341-346 行的 GLB 场景生成代码
3. `Transform::IDENTITY` 改为带缩放的版本：

```rust
// 改动前
Transform::IDENTITY,

// 改动后（例如放大 1.5 倍，上移 0.2）
Transform::from_xyz(0.0, 0.2, 0.0).with_scale(Vec3::new(1.5, 1.5, 1.5)),
```

4. 保存 → `cargo run` 测试
5. 观察城市模型是否变大，位置是否正确
6. 反复调参直到满意

#### 步骤 B：调整城市地图在 Y 轴的位置

如果城市模型和地面碰撞面不对齐（例如模型沉入地下或悬空）：

1. 打开 `ni/src/level.rs`
2. 找到第 341-346 行
3. 只调整 Y 轴：

```rust
// 上移 1.0 单位
Transform::from_xyz(0.0, 1.0, 0.0)
```

4. 如果模型高于地面，需要相应调整碰撞面（第 348-352 行）：

```rust
// 碰撞面也要上移对齐
Transform::from_xyz(0.0, 1.0, 0.0),  // 原来是 (0.0, 0.0, 0.0)
CollisionShape::Plane { y: 1.0 },     // 原来是 y: 0.0
```

#### 步骤 C：将 GLB 位置/缩放配置化（推荐长期方案）

1. 打开 `ni/src/level.rs`
2. 在 `ZoneDef` 结构体中添加 `glb_transform` 字段（约第 25 行之后）：

```rust
#[serde(default)]
pub glb_position: Option<(f32, f32, f32)>,
#[serde(default)]
pub glb_scale: Option<(f32, f32, f32)>,
```

3. 在 `spawn_zone()` 函数中，将 GLB 场景的 Transform 改为读配置：

```rust
// 替换 level.rs:343 的 Transform::IDENTITY
let glb_pos = zone.glb_position.unwrap_or((0.0, 0.0, 0.0));
let glb_scl = zone.glb_scale.unwrap_or((1.0, 1.0, 1.0));
Transform::from_xyz(glb_pos.0, glb_pos.1, glb_pos.2)
    .with_scale(Vec3::new(glb_scl.0, glb_scl.1, glb_scl.2)),
```

4. 在 RON 文件中配置：

```ron
// city.ron
glb_position: Some((0.0, 0.0, 0.0)),
glb_scale: Some((2.0, 2.0, 2.0)),
```

5. 保存 → `cargo check` → `cargo run` 测试

#### 步骤 D：调整程序化地面大小

这是最简单的操作：

1. 打开对应的 RON 文件（如 `assets/zones/grassland.ron`）
2. 修改 `floor_size` 值
3. 保存 → `cargo run` 查看效果
4. 注意：增大地面后可能需要调整物体分布，否则所有物体会挤在原点附近

#### 步骤 E：调整玩家出生点

1. 打开对应 RON 文件
2. 修改 `spawn_point` 坐标
3. 保存即可，无需改代码

#### 步骤 F：调试时注意事项

| 注意点 | 说明 |
|--------|------|
| GLB 坐标系 | 3D 建模软件的 Y-up vs Z-up 会影响模型朝向。如果模型倒了，调整 `Transform::from_rotation()` |
| 碰撞面同步 | 移动 GLB 场景后必须同步移动碰撞 Plane 的 Y 值 |
| scale 碰撞 | 仅缩放 GLB 不影响碰撞。缩放后玩家可能穿模，需要在 RON 中加 walls |
| floor_size 和 物体密度 | 面积 = floor_size²，物体数量不变时，面积越大越稀疏 |
| GLB 加载延迟 | GLB 异步加载，改变 Transform 后可能需要等 1~2 帧才看到效果 |
| Bevy 0.18 GLB 路径 | 必须带 `#Scene0` 标签（代码自动补全）。如果路径不含 `#`，代码会自动追加 |

---

### 11.5 当前 GLB 模型坐标参考

| 地图 | GLB 路径 | 文件大小 | Transform | 碰撞 |
|------|---------|---------|-----------|------|
| city | `models/city.glb#Scene0` | ~60MB | `IDENTITY` (原点, 1:1) | Plane{y:0.0} |
| city1 | `models/city1.glb` | ~19MB | 未使用 | - |

**注意**: `city1.glb` 较小（19MB vs 60MB），如果性能有问题可考虑换用。

---

### 11.6 地图视觉尺寸对比

由于 `city.glb` 是真实建模城市，而程序化地面只是一个平面：

| 地图 | 类型 | 视觉尺寸 | 视觉复杂度 |
|------|------|---------|-----------|
| grassland | 程序化平面 60×60 | 平坦草地 | 低（绿色平面+平台） |
| blue_forest | 程序化平面 80×80 | 平坦森林 | 低（蓝色平面） |
| dark_ruins | 程序化平面 60×60 + 墙壁 | 迷宫 | 中（墙壁构成通道） |
| city | GLB 模型 | 取决于 3D 模型 | 高（建筑/街道/细节） |

GLB 模型的"大小"由建模时的尺寸决定。如果 3D 模型本身是 10×10 的城市，即使 scale=1，地图也只有 10×10。可以用 `floor_size` 作为碰撞地面的参考尺寸。

---

## 十二、常见问题速查

| 问题 | 检查 |
|------|------|
| 角色太飘 | ↑ gravity, ↓ jump_force |
| 移动迟钝 | ↑ move_speed, ↑ rotation_speed |
| 跳跃太矮 | ↑ jump_force |
| 鼠标太快/慢 | ↓/↑ mouse_sensitivity |
| 抬头低头范围太小 | ↑ pitch_clamp (±1.5→±1.57) |
| 相机太近/远 | 改 camera_offset (0, 1.9, -1.0) |
| 场景太暗 | ↑ ambient.brightness, ↑ illuminance |
| 手电筒太暗 | ↑ flashlight_intensity, ↑ flashlight_range |
| 手电筒范围太窄 | ↑ outer_angle (0.5→0.8) |
| 阴影太硬 | ↑ ambient.brightness, ↓ illuminance |
| 描边太粗/细 | ↓/↑ outline_width |
| 高光太多 | ↑ spec_threshold (更接近1.0) |
| 收集品捡不到 | ↑ pickup_radius |
| 敌人一碰就死 | ↓ enemy_default_damage, ↑ health.max |
| 穿墙 | 检查 wall.scale 与碰撞半尺寸(0.5) |
| 跳不上平台 | 计算 jump_height ≥ platform_y |
| 模型不显示 | 确认 glb_scene 路径 + bevy_gltf feature |
| 区域切换不触发 | 检查 trigger_size 覆盖范围 + 玩家位置是否在触发区内 |
| 区域定义不加载 | 检查 assets/zones/*.ron 文件存在 + RON 语法正确 |
| GLB 建筑穿墙 | 为 GLB 区域添加 walls 配置，或在 ZoneDef 中为模型加碰撞体 |
| 切换区域后位置不对 | 检查 transitions[].spawn_point 坐标 |
| 多区域循环触发 | 检查 spawn_point 是否在目标区域触发区外 (≥24单位) |
| 关卡完成不触发 | 检查 collectibles 数量>0 + 全部收集 |
| 新区域无法开始 | 检查 GameLevel.zone_id() 与 .ron 文件 id 是否匹配 |
| zone 目录空 | 自动创建 `assets/zones/` 后启动，需手动添加 .ron 文件 |
| 地图模型太大/太小 | city.glb 尺寸由 3D 建模决定 | 调整 GLB Transform 的 scale，或回到 Blender 重新导出 |
| GLB 模型位置偏移 | 城市模型和地面不在同一平面 | 调整 `Transform::from_xyz(x, y, z)` 的 Y 值对齐地面 |
| 缩放后碰撞不对 | scale GLB 后玩家仍能穿过建筑 | 手动在 RON 添加 walls，碰撞面和缩放后的模型位置对齐 |
| 程序化地面太大物体太少 | 60×60 地面只有 3 个收集品 | 增加收集品/敌人数量，或减小 floor_size |
| 物体坐标超出地面 | 收集品/敌人在地面之外生成 | 所有物体的 x,z 坐标应在 ±(floor_size/2 - 1) 范围内 |
| Y 坐标不对导致浮空/沉入 | 物体漂浮或陷进地面 | 检查各物体 Y 坐标：地面 y=0，小物体 y≥0.2，NPC y=0 |
| 多次调参后忘记原始值 | 不记得原来设的什么值 | 每次调参前在计划文件中记录当前值 → 目标值 → 测试结果 |
